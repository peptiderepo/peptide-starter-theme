# OS files
.DS_Store
Thumbs.db

# Editor files
*.swp
*.swo
*~
.vscode/
.idea/

# Node (if ever used for build tooling)
node_modules/
package-lock.json

# Composer (dev dependencies only, not deployed)
vendor/
composer.lock

# Zip artifacts (built by CI)
*.zip

# Environment & credentials
.env
.env.local
.env.credentials

# AI agent context (local only, not deployed)
.claude/
